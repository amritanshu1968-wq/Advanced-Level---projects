Place your image files (jpg/png) here. The `npm run optimize-images` script will convert them to WebP and copy originals into `dist/assets`.

Example: add `sample.jpg` to this folder before running `npm run prepare`.
